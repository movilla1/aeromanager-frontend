import React, { Fragment } from 'react';
//import useToken from '../../shared/session';

function Main(props) {
  //const [token, setToken] = useToken();
  return (
    <Fragment>
      <h1>AeroManager</h1>
    </Fragment>
  );
}

export default Main;